# Large local data (download separately)
*.xlsx
*.xls
~$*
*.db
*.db-shm
*.db-wal

# Python
__pycache__/
*.py[cod]
.env
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Extension dev (keep committed gzip; ignore uncompressed export for size)
extension/data/employers.json

# Backend / Python services
backend/.venv/
data-pipeline/.venv/
*.egg-info/
# Trace files (keep logs/traces/.gitkeep)
logs/traces/*.json

