# Copy to .env and adjust as needed. .env is gitignored.

# Postgres (used by docker-compose and the backend)
POSTGRES_USER=jobintel
POSTGRES_PASSWORD=jobintel
POSTGRES_DB=jobintel
POSTGRES_PORT=5432

# Backend database connection
# Local (running uvicorn on host):     host is localhost
# In docker-compose (backend service): host is db
DATABASE_URL=postgresql://jobintel:jobintel@localhost:5432/jobintel
